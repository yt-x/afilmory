# Afilmory 环境变量配置示例
# 复制此文件为 .env 并填入你的真实配置

# ========================================
# 阿里云 OSS 配置 (必填)
# ========================================

# AccessKey ID (从阿里云控制台获取)
S3_ACCESS_KEY_ID=your_access_key_id_here

# AccessKey Secret (从阿里云控制台获取)
S3_SECRET_ACCESS_KEY=your_access_key_secret_here

# Bucket 名称
S3_BUCKET_NAME=your_bucket_name

# Region - 示例:
# - 华东1(杭州): oss-cn-hangzhou
# - 华北2(北京): oss-cn-beijing
# - 华南1(深圳): oss-cn-shenzhen
S3_REGION=oss-cn-hangzhou

# Endpoint - 必须与 Region 匹配,格式: https://oss-{region}.aliyuncs.com
S3_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com

# OSS 中图片的路径前缀 (可选,留空表示根目录)
S3_PREFIX=

# 自定义域名 (可选,如果配置了 OSS 自定义域名)
S3_CUSTOM_DOMAIN=

# 排除正则表达式 (可选,用于排除特定文件)
S3_EXCLUDE_REGEX=

# ========================================
# 可选配置
# ========================================

# Git Token - 用于将构建结果推送到远程仓库实现缓存 (可选)
GIT_TOKEN=

# PostgreSQL 数据库连接字符串 (可选)
PG_CONNECTION_STRING=

# ========================================
# 重要提示
# ========================================
# 1. 等号两边不要有空格
# 2. 不要添加引号
# 3. 确保 Region 和 Endpoint 匹配
# 4. 保存后请确保文件名为 .env (不是 .env.example)
